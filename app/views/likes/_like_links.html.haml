%div.like-link{id: "like-link-#{item.id}"}
  = fa_icon 'heart' 
  - if current_user.likes.find_by(item_id: item.id)
    = link_to unlike_path(item.id), method: :delete, remote: true do
      .btn.glyphicon.glyphicon-heart{id: "heart-#{item.id}"}
        = item.likes.count.to_s
  - else
    = link_to like_path(item.id), method: :post, remote: true do
      .btn.glyphicon.glyphicon-heart-empty{id: "heart-#{item.id}"}
        = item.likes.count.to_s