.container
  %header.header
    %h1
      = link_to root_path do
        - if session[:tosaben] == "true"
          = image_tag 'tosaben-logo.png', class: 'logo tosaben-logo'
        - else
          = image_tag 'logo.png', class: 'logo'
    %nav.signup-bar
      %oi.clearfix
        %li.through
          会員情報
          .progress-status
        %li.active
          電話番号認証
          .progress-status
        %li
          お届け先住所入力
          .progress-status
        %li
          支払い方法
          .progress-status
        %li
          完了
          .progress-status

  %main.main
    %section.container-main
      %h2.container-main__head
        電話番号の確認
      %hr
      = form_for @user, url: sms_confirmation_sms_signup_index_path, method: :get, html: {class: 'new_user'} do |f|
        .container-main__inner__content
          .form-group
            .field
              = f.label "携帯電話の番号"
              = f.text_field :phone_number, placeholder: "携帯電話の番号を入力", class: 'input-default'
            %p.container-main__text 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
          .form-group
            %button.btn-default.btn-red{type: "submit"} 次へ進む
            %p.container-main__text ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
          .form-group
            %p.container-main__text-right
              %a{href:"https://www.mercari.com/jp/help_center/article/423/"} 電話番号の確認が必要な理由＞
      
      = render 'devise/shared/footer'